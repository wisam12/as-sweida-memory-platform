
{
  "name": "as-sweida-memory-platform",
  "version": "1.0.0",
  "description": "Platform to preserve the memory of the As-Sweida massacre",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "vite": "^4.0.0",
    "@vitejs/plugin-react": "^4.0.0"
  }
}
